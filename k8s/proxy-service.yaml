apiVersion: v1
kind: Service
metadata:
  name: headroom-proxy
  namespace: headroom
  labels:
    app: headroom-proxy
spec:
  selector:
    app: headroom-proxy
  ports:
  - name: http
    port: 8787
    targetPort: 8787
    nodePort: 30787
  type: NodePort
